RewriteEngine on
# 301 --- http://www.test.com/ => http://www.test.com/spiders.html
RewriteRule ^$ /phpmyfaq/? [R=301]

RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R,L]
#RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R,L]

